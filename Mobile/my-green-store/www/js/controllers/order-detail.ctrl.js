(function () {
  'use strict';
  function OrderDetailController($state) {
    var vm = this;

    vm.init = function () {
      
    };

    vm.init();
  }

  angular.module('mgstore')
    .controller('OrderDetailController', ['$state', OrderDetailController]);
} ());